apiVersion: projectcontour.io/v1
kind: HTTPProxy
metadata:
  name: notification-service
  namespace: hiring-system
spec:
  routes:
    - loadBalancerPolicy:
        strategy: WeightedLeastRequest
      services:
        - name: notification-service
          port: 80
      timeoutPolicy:
        idle: infinity
  virtualhost:
    corsPolicy:
      allowCredentials: true
      allowHeaders:
        - '*'
      allowMethods:
        - GET
        - POST
        - OPTIONS
        - PUT
        - DELETE
        - PATCH
      allowOrigin:
        - '*'
      exposeHeaders:
        - Content-Length
        - Content-Range
      maxAge: 10m
    fqdn: hiring-notification-service.wohlig.com
